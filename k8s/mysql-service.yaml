apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: task-manager
spec:
  selector:
    app: mysql              # Routes traffic to pods with this label
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None           # "Headless" service — direct pod DNS, best for databases